s/return \$page->pagetype === 'my-index'\n            && \$page->pagelayout === 'mydashboard'\n            && \$page->blocks->is_block_present\('compass'\);/return \$page->pagetype === 'my-index' && \$page->pagelayout === 'mydashboard';/;
