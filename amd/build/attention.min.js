define("block_compass/attention",["exports","core/templates","block_compass/repository"],(function(_exports,_templates,_repository){var obj;
/**
   * Renders tier 1 into the shell.
   *
   * @module     block_compass/attention
   * @copyright  2026 Anderson Blaine
   * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
   */Object.defineProperty(_exports,"__esModule",{value:!0}),_exports.render=void 0,_templates=(obj=_templates)&&obj.__esModule?obj:{default:obj};const SELECTORS_strip="[data-strip]",SELECTORS_cards='[data-region="cards"]',SELECTORS_tier1='[data-region="tier1"]',SELECTORS_ghost='[data-region="ghost"]',SELECTORS_empty='[data-region="empty"]',SELECTORS_progress='[data-region="progress"]',STRIPS=["continue","new","favourites"],renderStrip=async(strip,cards,config,ghost)=>{const list=strip.querySelector(SELECTORS_cards),context={cards:cards.map((card=>({...card,favouritesenabled:config.favouritesenabled}))),ghost:ghost||!1},rendered=await _templates.default.renderForPromise("block_compass/cards",context);_templates.default.replaceNodeContents(list,rendered.html,rendered.js),strip.hidden=!1},fillPending=async(root,courseids,labels)=>{if(!courseids.length)return;const renders=(await(0,_repository.getCardDetails)(courseids.slice(0,24))).details.map((async detail=>{const region=root.querySelector("".concat(SELECTORS_progress,'[data-course-id="').concat(detail.id,'"]'));if(!region)return;if(!detail.hascompletion||null===detail.progress)return void(region.textContent=labels.nocompletion||"");const rendered=await _templates.default.renderForPromise("block_compass/progress",{progress:detail.progress,iscomplete:detail.progress>=100});_templates.default.replaceNodeContents(region,rendered.html,rendered.js)}));await Promise.all(renders),courseids.length>24&&await fillPending(root,courseids.slice(24),labels)};_exports.render=async(root,data,config)=>{const labels=config.labels||{},tier1=root.querySelector(SELECTORS_tier1),ghostwrap=root.querySelector(SELECTORS_ghost),empty=root.querySelector(SELECTORS_empty);let shown=0;for(const name of STRIPS){const strip=root.querySelector("".concat(SELECTORS_strip,'[data-strip="').concat(name,'"]')),cards=data[name]||[];if(!strip||!cards.length)continue;let ghost=null;"new"===name&&data.counts.newmore>0?ghost={count:data.counts.newmore,text:labels.ghost_more_new,kind:"new"}:"favourites"===name&&data.counts.favouritesmore>0&&(ghost={count:data.counts.favouritesmore,text:labels.ghost_more_favourites,kind:"favourites"}),await renderStrip(strip,cards,config,ghost),shown+=cards.length}if(tier1.hidden=0===shown,data.counts.more>0){const ghost={count:data.counts.more,text:labels.ghost_more||"",cta:labels.ghost_explore||"",kind:"tier2"},rendered=await _templates.default.renderForPromise("block_compass/tier2",{count:ghost.count,text:ghost.text,props:JSON.stringify(ghost)});_templates.default.replaceNodeContents(ghostwrap,rendered.html,rendered.js),ghostwrap.hidden=!1}0===shown&&(empty.textContent=0===data.counts.total?labels.nocourses:labels.emptyattention,empty.hidden=!1);const pending=STRIPS.flatMap((name=>(data[name]||[]).filter((card=>card.pending)).map((card=>card.id))));await fillPending(root,pending,labels)}}));

//# sourceMappingURL=attention.min.js.map